(()=>{'use strict';const W=960;const H=640;const TAU=Math.PI*2;const FIXED_DT=1/120;const VERSION='0.8.1';const BEST_KEY='sid.sylvaria.countercut.v8.best';const canvas=document.getElementById('c');const ctx=canvas?.getContext('2d');if(!canvas||!ctx)return;const $=(id)=>document.getElementById(id);const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));const lerp=(a,b,t)=>a+(b-a)*t;const dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);const normKey=(key)=>String(key||'').toLowerCase();const easeOut=(t)=>1-Math.pow(1-clamp(t,0,1),3);const alpha=(hex,opacity)=>{let raw=String(hex||'#ffffff').replace('#','');if(raw.length===3)raw=raw.split('').map((c)=>c+c).join('');const n=Number.parseInt(raw,16)||0xffffff;return `rgba(${n >> 16},${(n >> 8) & 255},${n & 255},${opacity})`;};const hash=(value)=>{let result=2166136261;for(const ch of String(value)){result^=ch.charCodeAt(0);result=Math.imul(result,16777619);}return result>>>0;};const rngFrom=(seed)=>{let value=seed>>>0;return()=>{value+=0x6d2b79f5;let t=value;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return((t^(t>>>14))>>>0)/4294967296;};};const entityRand=(entity)=>{let x=(entity.rngState||0x9e3779b9)>>>0;x^=x<<13;x^=x>>>17;x^=x<<5;entity.rngState=x>>>0;return entity.rngState/4294967296;};const DIRS=Object.freeze({up:{x:0,y:-1,key:'arrowup',glyph:'↑'},down:{x:0,y:1,key:'arrowdown',glyph:'↓'},left:{x:-1,y:0,key:'arrowleft',glyph:'←'},right:{x:1,y:0,key:'arrowright',glyph:'→'},});const MOVE_DIRS=Object.freeze({w:DIRS.up,s:DIRS.down,a:DIRS.left,d:DIRS.right});const CUT_KEYS=Object.freeze({arrowup:'up',arrowdown:'down',arrowleft:'left',arrowright:'right'});const ENEMY_TYPES=Object.freeze({feller:{name:'Rookie Feller',hp:3,speed:52,r:16,color:'#ffb36b',reward:90,blurb:'Commits to a broad axe mark. Leave before the swing lands.',},foreman:{name:'Nailgun Foreman',hp:4,speed:42,r:17,color:'#ff7f66',reward:120,blurb:'Fires readable nail lanes that teach directional counters.',},lobbyist:{name:'Timber Lobbyist',hp:4,speed:38,r:16,color:'#d698ff',reward:145,blurb:'Launches red tape. Return it or your movement cadence gets buried in procedure.',},skidder:{name:'Skidder Bruiser',hp:7,speed:34,r:22,color:'#ffc85c',reward:190,blurb:'Armored until its committed charge ends in a punish window.',},drone:{name:'Harvester Drone',hp:5,speed:66,r:15,color:'#7de6ff',reward:170,blurb:'Curves saw-discs around the grove and changes the arrival side late.',},chair:{name:'Committee Chair',hp:6,speed:30,r:18,color:'#8ea2ff',reward:220,blurb:'Projects an actual damage shield around nearby cutters. Yes, the meeting has quorum.',},broker:{name:'Subsidy Broker',hp:5,speed:46,r:17,color:'#f6e17b',reward:200,blurb:'Fires boost-coins at allies. Intercept the transfer before it lands.',},mech:{name:'Clearcut Mech',hp:10,speed:27,r:25,color:'#ff6f6f',reward:290,blurb:'Heavy mixed pressure with deterministic tree-targeting bursts.',},});const ROOM_BLUEPRINTS=Object.freeze([{title:'Trailhead Trespass',subtitle:'learn the step-dash',palette:['#07160f','#123a24','#79ef91','#f4e4a5'],trees:5,deadwood:4,enemies:['feller','feller'],dash:48,hint:'Tap WASD to step-dash. Arrow keys cut. Axe marks are promises, not suggestions.',},{title:'Nailgun Nursery',subtitle:'first real counters',palette:['#061713','#0d4634','#70ffd0','#ffc274'],trees:5,deadwood:5,enemies:['foreman','feller','foreman'],dash:50,hint:'Cut toward the side a nail reaches you from. White flash = perfect counter.',},{title:'Red Tape Ravine',subtitle:'bureaucracy has projectiles',palette:['#101322','#263866','#a9baff','#ff87bf'],trees:6,deadwood:5,enemies:['lobbyist','foreman','feller'],dash:52,hint:'Red tape slows held-step cadence. Counter it and send the paperwork back.',},{title:'Skidder Switchback',subtitle:'commitment beats armor',palette:['#171107','#4c3212','#f7cc69','#7bffa4'],trees:6,deadwood:6,enemies:['skidder','feller','foreman'],dash:54,hint:'The Skidder locks its charge line. Step aside, then punish the recovery window.',},{title:'Sawdisc Wetland',subtitle:'moving counter geometry',palette:['#04141a','#07556b','#78e7ff','#ffba8c'],trees:7,deadwood:6,enemies:['drone','drone','foreman'],dash:56,hint:'Saw-discs curve. Counter the side they reach, not the side they started on.',},{title:'Committee Canopy',subtitle:'break the support network',palette:['#0c1022','#263570','#9cafef','#e4d2ff'],trees:7,deadwood:7,enemies:['chair','lobbyist','foreman'],dash:58,hint:'The Chair reduces nearby damage. Remove the committee before grinding the members.',},{title:'Subsidy Grove',subtitle:'intercept enemy synergy',palette:['#161707','#52500e','#fff078','#ff8b6f'],trees:8,deadwood:7,enemies:['broker','skidder','feller','foreman'],dash:60,hint:'Gold transfers accelerate allies only if they land. Cut the funding stream mid-flight.',},{title:'Clearcut Conveyor',subtitle:'protect moving priorities',palette:['#160a0a','#562020','#ff786e','#e2f58a'],trees:8,deadwood:8,enemies:['mech','drone','foreman','feller'],dash:62,hint:'The Mech telegraphs whether it wants Sprid or a tree. Believe the line and move early.',},{title:'Four-Way Firebreak',subtitle:'counter fluency check',palette:['#07110f','#284835','#8dff9b','#ffb568'],trees:9,deadwood:9,enemies:['lobbyist','drone','skidder','foreman','chair'],dash:64,hint:'Movement and cut direction are independent. Dash east while defending north.',},{title:'PAC-a-Saw Summit',subtitle:'boss · fully subsidized',palette:['#050914','#1d2854','#89d8ff','#ff6f92'],trees:10,deadwood:10,enemies:[],dash:66,boss:true,hint:'Three phases. Read the locked intent, counter the barrage, punish the recovery glow.',},]);const state={mode:'menu',runMode:'run',worldDepth:1,roomIndex:0,worldsCleared:0,score:0,totalTime:0,roomTime:0,fps:60,player:null,room:null,enemies:[],shots:[],particles:[],slashes:[],trees:[],debris:[],boss:null,heldMoves:new Set(),heldOrder:[],moveBuffer:null,moveRepeatTimer:0,roomClearTimer:0,slowTimer:0,flash:0,shake:0,blockedHint:null,best:readBest(),muted:false,stats:freshStats(),};function freshStats(){return{cuts:0,hits:0,counters:0,perfectCounters:0,kills:0,treesSaved:0,deadwood:0,dashes:0,damageTaken:0,grazes:0,blockedSteps:0,fullGroves:0,};}function readBest(){try{return JSON.parse(localStorage.getItem(BEST_KEY)||'null')||{room:0,score:0};}catch{return{room:0,score:0};}}function writeBest(){const next={room:Math.max(state.best.room||0,state.worldDepth),score:Math.max(state.best.score||0,Math.floor(state.score)),};state.best=next;try{localStorage.setItem(BEST_KEY,JSON.stringify(next));}catch{}}class CountercutAudio{constructor(){this.ctx=null;}ensure(){if(state.muted)return null;if(!this.ctx){const AudioContext=window.AudioContext||window.webkitAudioContext;if(!AudioContext)return null;this.ctx=new AudioContext();}if(this.ctx.state==='suspended')this.ctx.resume().catch(()=>{});return this.ctx;}tone(freq,duration=.05,type='sine',gain=.035,slide=0){const audioCtx=this.ensure();if(!audioCtx)return;const now=audioCtx.currentTime;const osc=audioCtx.createOscillator();const amp=audioCtx.createGain();osc.type=type;osc.frequency.setValueAtTime(freq,now);if(slide)osc.frequency.exponentialRampToValueAtTime(Math.max(30,freq+slide),now+duration);amp.gain.setValueAtTime(gain,now);amp.gain.exponentialRampToValueAtTime(.0001,now+duration);osc.connect(amp).connect(audioCtx.destination);osc.start(now);osc.stop(now+duration+.01);}dash(){this.tone(190,.045,'triangle',.028,80);}cut(){this.tone(510,.035,'sawtooth',.02,-180);}counter(perfect){this.tone(perfect?940:720,.055,'triangle',.045,perfect?380:120);if(perfect)this.tone(1320,.07,'sine',.025,120);}hurt(){this.tone(95,.11,'square',.045,-25);}room(){this.tone(520,.08,'triangle',.03,240);this.tone(780,.11,'sine',.025,280);}phase(){this.tone(145,.12,'sawtooth',.04,-35);}}const audio=new CountercutAudio();function pressure(depth=state.worldDepth){if(depth<=1)return.72;if(depth===2)return.82;return clamp(.88+Math.log2(depth+1)*.08,.88,1.55);}function proceduralBlueprint(depth){const rng=rngFrom(hash(`sylvaria-countercut-room-${depth}`));const roster=['feller','foreman','lobbyist','skidder','drone','chair','broker','mech'];const count=clamp(4+Math.floor(Math.log2(depth+1)*.7),4,8);const enemies=[];let supportCount=0;while(enemies.length<count){const candidate=roster[Math.floor(rng()*roster.length)];const support=candidate==='chair'||candidate==='broker';if(support&&supportCount>=2)continue;if(support)supportCount+=1;enemies.push(candidate);}const palette=ROOM_BLUEPRINTS[Math.floor(rng()*ROOM_BLUEPRINTS.length)].palette;return{title:`Wild Sector ${String(depth).padStart(3, '0')}`,subtitle:depth%10===0?'seeded guardian clearcut':'seeded forest counter-scenario',palette,trees:clamp(7+Math.floor(depth/7),7,12),deadwood:clamp(5+Math.floor(depth/9),5,12),enemies:depth%10===0?enemies.slice(0,Math.max(2,enemies.length-2)):enemies,dash:clamp(66+Math.floor((depth-10)/4)*3,66,104),hint:'Longer steps cross more danger but make every commitment harder to retract.',boss:depth%10===0,};}function roomBlueprint(depth){return depth<=10?ROOM_BLUEPRINTS[depth-1]:proceduralBlueprint(depth);}function randomPoint(rng,margin=70){return{x:margin+rng()*(W-margin*2),y:105+rng()*(H-175)};}function pointClear(point,radius,existing,pad=14){return existing.every((item)=>dist(point,item)>radius+item.r+pad);}function placeObject(rng,radius,existing,preferRight=false){for(let attempt=0;attempt<80;attempt+=1){const point=randomPoint(rng,78);if(preferRight)point.x=Math.max(280,point.x);if(point.x<210&&Math.abs(point.y-H/2)<90)continue;if(pointClear(point,radius,existing))return point;}return{x:preferRight?520:360,y:140+(existing.length%6)*72};}function setupRoom(depth,carry=null){const blueprint=roomBlueprint(depth);const rng=rngFrom(hash(`sylvaria-countercut-layout-${depth}`));state.worldDepth=depth;state.roomIndex=(depth-1)%10;state.room={...blueprint,seed:hash(`sylvaria-countercut-layout-${depth}`)};const carryHp=carry?.hp??5;const carryFlow=carry?.flow??0;state.player={x:108,y:H/2,r:14,hp:clamp(carryHp,1,5),maxHp:5,flow:clamp(carryFlow,0,100),facing:'right',cutDirection:'right',dash:null,dashCooldown:0,cutCooldown:0,invuln:0,dashDistance:blueprint.dash,trail:[],};state.enemies=[];state.shots=[];state.particles=[];state.slashes=[];state.trees=[];state.debris=[];state.boss=null;state.heldMoves.clear();state.heldOrder=[];state.moveBuffer=null;state.moveRepeatTimer=0;state.roomTime=0;state.roomClearTimer=0;state.slowTimer=0;state.flash=0;state.shake=0;state.blockedHint=null;const placed=[];for(let i=0;i<blueprint.trees;i+=1){const r=22+rng()*6;const point=placeObject(rng,r,placed,true);const tree={...point,r,hp:5,maxHp:5,alive:true,phase:rng()*TAU,id:`tree-${depth}-${i}`};state.trees.push(tree);placed.push(tree);}for(let i=0;i<blueprint.deadwood;i+=1){const r=15+rng()*7;const point=placeObject(rng,r,placed,false);const debris={...point,r,hp:2,dead:false,angle:rng()*TAU,id:`deadwood-${depth}-${i}`};state.debris.push(debris);placed.push(debris);}blueprint.enemies.forEach((type,index)=>spawnEnemy(type,rng,index,placed));if(blueprint.boss)spawnBoss();updateHud(true);}function spawnEnemy(type,rng,index=0,placed=[]){const spec=ENEMY_TYPES[type];const point=placeObject(rng,spec.r,placed,true);const hpScale=1+Math.max(0,state.worldDepth-10)*.024;const hp=Math.ceil(spec.hp*hpScale);const enemy={id:`${type}-${state.worldDepth}-${index}`,type,x:point.x,y:point.y,r:spec.r,hp,maxHp:hp,clock:.8+rng()*.7,telegraph:0,state:'move',phase:rng()*TAU,angle:rng()*TAU,armor:type==='skidder'?1:0,boosted:0,intent:null,attackCount:0,treeClock:.5,rngState:hash(`${state.room.seed}:${type}:${index}`),hitFlash:0,dead:false,};state.enemies.push(enemy);placed.push(enemy);}function spawnBoss(){const maxHp=38+Math.max(0,state.worldDepth-10)*2;state.boss={id:`pac-a-saw-${state.worldDepth}`,type:'boss',name:'PAC-a-Saw',x:720,y:H/2,r:42,hp:maxHp,maxHp,phase:1,clock:1.25,telegraph:0,recover:0,state:'move',angle:0,attackCount:0,intent:null,rngState:hash(`pac-a-saw:${state.worldDepth}`),hitFlash:0,dead:false,slogan:'FULLY SUBSIDIZED · MINIMALLY ACCOUNTABLE',};}function blockers(){return[...state.trees.filter((tree)=>tree.alive).map((tree)=>({...tree,kind:'tree'})),...state.debris.filter((item)=>!item.dead).map((item)=>({...item,kind:'deadwood'})),];}function resolveDashTarget(startX,startY,dir,distance){let lastX=startX;let lastY=startY;let blockedBy=null;const steps=Math.max(1,Math.ceil(distance/4));for(let i=1;i<=steps;i+=1){const amount=distance*(i/steps);const x=clamp(startX+dir.x*amount,28,W-28);const y=clamp(startY+dir.y*amount,80,H-32);const hit=blockers().find((item)=>Math.hypot(x-item.x,y-item.y)<state.player.r+item.r+3);if(hit){blockedBy=hit;break;}lastX=x;lastY=y;}return{x:lastX,y:lastY,blockedBy};}function repeatCadence(){const flowBenefit=lerp(.175,.138,clamp(state.player.flow/100,0,1));return state.slowTimer>0?flowBenefit*1.55:flowBenefit;}function requestDash(key,buffer=true){const dir=MOVE_DIRS[key];if(!dir||state.mode!=='playing')return false;if(dashStep(dir,key))return true;if(buffer)state.moveBuffer={key,life:.13};return false;}function dashStep(dir,sourceKey=null){const p=state.player;if(!p||state.mode!=='playing'||p.dash||p.dashCooldown>0)return false;const resolved=resolveDashTarget(p.x,p.y,dir,p.dashDistance);const travelled=Math.hypot(resolved.x-p.x,resolved.y-p.y);if(travelled<12){p.dashCooldown=.055;state.stats.blockedSteps+=1;state.blockedHint=resolved.blockedBy?{x:resolved.blockedBy.x,y:resolved.blockedBy.y,life:.28}:null;state.shake=Math.max(state.shake,2.5);audio.tone(120,.045,'square',.02,-30);return false;}p.dash={sx:p.x,sy:p.y,tx:resolved.x,ty:resolved.y,t:0,duration:clamp(.095+travelled/2400,.10,.145),dir,blockedBy:resolved.blockedBy,};p.dashCooldown=.03;p.facing=dir.x<0?'left':dir.x>0?'right':p.facing;state.stats.dashes+=1;state.moveRepeatTimer=repeatCadence();state.shake=Math.max(state.shake,1.2);for(let i=0;i<4;i+=1)spawnParticle(p.x-dir.x*i*5,p.y-dir.y*i*5,'#8dff9b',8+i*3);if(sourceKey)state.lastMoveKey=sourceKey;audio.dash();return true;}function updateMovement(dt){const p=state.player;if(!p)return;if(p.dashCooldown>0)p.dashCooldown-=dt;if(state.moveRepeatTimer>0)state.moveRepeatTimer-=dt;if(state.moveBuffer){state.moveBuffer.life-=dt;if(state.moveBuffer.life<=0)state.moveBuffer=null;}if(p.dash){p.dash.t+=dt;const t=clamp(p.dash.t/p.dash.duration,0,1);const eased=easeOut(t);p.x=lerp(p.dash.sx,p.dash.tx,eased);p.y=lerp(p.dash.sy,p.dash.ty,eased);p.trail.push({x:p.x,y:p.y,life:.18});if(p.trail.length>12)p.trail.shift();if(t>=1){const blocked=p.dash.blockedBy;p.dash=null;if(blocked)state.blockedHint={x:blocked.x,y:blocked.y,life:.24};}return;}if(state.moveBuffer&&p.dashCooldown<=0){const buffered=state.moveBuffer.key;state.moveBuffer=null;if(requestDash(buffered,false))return;}const active=[...state.heldOrder].reverse().find((key)=>state.heldMoves.has(key));if(active&&p.dashCooldown<=0&&state.moveRepeatTimer<=0)requestDash(active,false);}function cut(direction){const p=state.player;if(!p||state.mode!=='playing'||p.cutCooldown>0)return false;const d=DIRS[direction];const flow=clamp(p.flow/100,0,1);p.cutCooldown=lerp(.19,.158,flow);p.cutDirection=direction;p.facing=d.x<0?'left':d.x>0?'right':p.facing;state.stats.cuts+=1;state.slashes.push({direction,x:p.x,y:p.y,age:0,life:state.worldDepth<=2?.145:.132,perfectWindow:clamp(.086-Math.max(0,state.worldDepth-1)*.0014,.062,.086),reach:78,width:58,hits:new Set(),});state.shake=Math.max(state.shake,1.7);spawnParticle(p.x+d.x*34,p.y+d.y*34,'#d9ffd7',20);audio.cut();return true;}function slashContains(slash,object){const d=DIRS[slash.direction];const ox=object.x-slash.x;const oy=object.y-slash.y;const along=ox*d.x+oy*d.y;const side=Math.abs(ox*-d.y+oy*d.x);return along>=4-object.r&&along<=slash.reach+object.r&&side<=slash.width*.5+object.r;}function shotApproachDirection(shot){const dx=shot.x-state.player.x;const dy=shot.y-state.player.y;if(Math.abs(dx)>Math.abs(dy))return dx<0?'left':'right';return dy<0?'up':'down';}function counterShot(shot,slash){if(shot.dead||shot.friendly)return false;const approach=shotApproachDirection(shot);if(approach!==slash.direction||!slashContains(slash,shot))return false;const d=DIRS[slash.direction];const perfect=slash.age<=slash.perfectWindow;shot.friendly=true;shot.owner=null;shot.beneficiaryId=null;shot.vx=d.x*(perfect?700:580);shot.vy=d.y*(perfect?700:580);shot.damage=perfect?3.2:2;shot.counterQuality=perfect?'perfect':'normal';shot.color=perfect?'#fffce0':'#99ffb3';shot.life=1.35;state.stats.counters+=1;if(perfect)state.stats.perfectCounters+=1;state.player.flow=clamp(state.player.flow+(perfect?18:10),0,100);state.score+=perfect?120:70;state.flash=Math.max(state.flash,perfect?.16:.06);state.shake=Math.max(state.shake,perfect?5:2.5);for(let i=0;i<(perfect?12:7);i+=1)spawnParticle(shot.x,shot.y,perfect?'#fff8bb':'#93ffb2',30+i*2);audio.counter(perfect);return true;}function shieldProvider(enemy){if(!enemy||enemy.type==='chair')return null;return state.enemies.find((candidate)=>!candidate.dead&&candidate.type==='chair'&&dist(candidate,enemy)<=165)||null;}function damageEnemy(enemy,amount,hitDir=null,source={}){if(!enemy||enemy.dead)return;const shield=shieldProvider(enemy);if(shield)amount*=source.counter?.78:.52;if(enemy.type==='skidder'&&enemy.armor>0&&enemy.state!=='recover')amount*=.25;enemy.hp-=amount;enemy.hitFlash=.12;state.stats.hits+=1;if(hitDir){enemy.x=clamp(enemy.x+hitDir.x*8,34,W-34);enemy.y=clamp(enemy.y+hitDir.y*8,88,H-35);}if(enemy.hp<=0){enemy.dead=true;state.stats.kills+=1;state.score+=(ENEMY_TYPES[enemy.type]?.reward||250)+state.player.flow*2;state.player.flow=clamp(state.player.flow+8,0,100);for(let i=0;i<14;i+=1)spawnParticle(enemy.x,enemy.y,ENEMY_TYPES[enemy.type]?.color||'#ff8f73',24+i*2);}}function damageBoss(amount,hitDir=null,source={}){const boss=state.boss;if(!boss||boss.dead)return;const armored=boss.state!=='recover'&&boss.phase>=2;let applied=armored?amount*.42:amount;if(source.counter&&boss.state==='recover')applied*=1.28;boss.hp-=applied;boss.hitFlash=.13;if(hitDir){boss.x+=hitDir.x*5;boss.y+=hitDir.y*5;}if(boss.hp<=boss.maxHp*.66&&boss.phase===1){boss.phase=2;boss.clock=.8;audio.phase();toast('PAC-a-Saw phase 2 · paperwork carousel');}if(boss.hp<=boss.maxHp*.33&&boss.phase===2){boss.phase=3;boss.clock=.65;audio.phase();toast('PAC-a-Saw phase 3 · emergency clearcut authority');}if(boss.hp<=0){boss.dead=true;state.score+=1750;state.stats.kills+=1;for(let i=0;i<48;i+=1)spawnParticle(boss.x,boss.y,i%2?'#ff7b9b':'#91f7ff',35+i);}}function damagePlayer(amount,source=null){const p=state.player;if(!p||p.invuln>0||state.mode!=='playing')return;p.hp-=amount;p.invuln=.66;p.flow=Math.max(0,p.flow-25);state.stats.damageTaken+=amount;state.shake=8;state.flash=.26;audio.hurt();if(source){const dx=p.x-source.x;const dy=p.y-source.y;const mag=Math.hypot(dx,dy)||1;p.x=clamp(p.x+dx/mag*16,28,W-28);p.y=clamp(p.y+dy/mag*16,80,H-32);}if(p.hp<=0)endRun('Sprid was overwhelmed');}function damageTree(tree,amount){if(!tree?.alive)return;tree.hp-=amount;state.shake=Math.max(state.shake,2);for(let i=0;i<6;i+=1)spawnParticle(tree.x,tree.y,'#c99b5d',18+i*2);if(tree.hp<=0){tree.alive=false;tree.hp=0;if(!state.trees.some((candidate)=>candidate.alive))endRun('The grove was clear-cut');}}function nearestLivingTree(from){let best=null;let bestDistance=Infinity;for(const tree of state.trees){if(!tree.alive)continue;const distance=dist(from,tree);if(distance<bestDistance){best=tree;bestDistance=distance;}}return best;}function moveToward(enemy,target,speed,dt){if(!target)return;const dx=target.x-enemy.x;const dy=target.y-enemy.y;const magnitude=Math.hypot(dx,dy)||1;enemy.x+=dx/magnitude*speed*dt;enemy.y+=dy/magnitude*speed*dt;}function fireShotToPoint(enemy,point,kind='nail',speed=330,spread=0,extra={}){if(!point)return;const dx=point.x-enemy.x;const dy=point.y-enemy.y;const base=Math.atan2(dy,dx)+spread;const colors={nail:'#ffad79',tape:'#dba0ff',saw:'#75e7ff',paper:'#a9baff',coin:'#ffe56e',boss:'#ff6f92',};state.shots.push({x:enemy.x,y:enemy.y,vx:Math.cos(base)*speed,vy:Math.sin(base)*speed,r:kind==='saw'?9:kind==='coin'?7:6,life:3,kind,color:colors[kind]||'#ffffff',friendly:false,owner:enemy,damage:1,dead:false,spin:0,grazed:false,...extra,});}function telegraphDuration(enemy){const early=state.worldDepth<=2?.10:0;const base=enemy.type==='skidder'?.56:enemy.type==='feller'?.42:.46;return clamp(base+early-Math.log2(state.worldDepth+1)*.012,.30,.66);}function chooseIntent(enemy){const p=state.player;const tree=nearestLivingTree(enemy);if(enemy.type==='feller'){if(tree&&dist(enemy,tree)<95)return{kind:'tree-melee',x:tree.x,y:tree.y,treeId:tree.id};return{kind:'player-melee',x:p.x,y:p.y};}if(enemy.type==='skidder')return{kind:'charge',x:p.x,y:p.y};if(enemy.type==='broker'){const candidates=state.enemies.filter((candidate)=>!candidate.dead&&candidate!==enemy&&candidate.type!=='broker');if(candidates.length){const beneficiary=candidates[Math.floor(entityRand(enemy)*candidates.length)];return{kind:'subsidy',x:beneficiary.x,y:beneficiary.y,beneficiaryId:beneficiary.id};}}if(enemy.type==='mech'){if(tree&&entityRand(enemy)<.58)return{kind:'tree-shot',x:tree.x,y:tree.y,treeId:tree.id};return{kind:'player-shot',x:p.x,y:p.y};}return{kind:'player-shot',x:p.x,y:p.y};}function beginTelegraph(enemy){enemy.intent=chooseIntent(enemy);enemy.telegraph=telegraphDuration(enemy);enemy.state='telegraph';}function enemyAttack(enemy){const intent=enemy.intent||chooseIntent(enemy);const target={x:intent.x,y:intent.y};enemy.attackCount+=1;if(enemy.type==='feller'){if(intent.kind==='tree-melee'){const tree=state.trees.find((candidate)=>candidate.id===intent.treeId&&candidate.alive);if(tree&&Math.hypot(enemy.x-target.x,enemy.y-target.y)<enemy.r+tree.r+24)damageTree(tree,1);}else if(Math.hypot(state.player.x-target.x,state.player.y-target.y)<34){damagePlayer(1,enemy);}}else if(enemy.type==='foreman'){fireShotToPoint(enemy,target,'nail',355);if(state.worldDepth>=5){fireShotToPoint(enemy,target,'nail',355,-.12);fireShotToPoint(enemy,target,'nail',355,.12);}}else if(enemy.type==='lobbyist'){fireShotToPoint(enemy,target,'tape',280);}else if(enemy.type==='drone'){fireShotToPoint(enemy,target,'saw',300);}else if(enemy.type==='chair'){fireShotToPoint(enemy,target,'paper',250);}else if(enemy.type==='broker'){const beneficiary=state.enemies.find((candidate)=>candidate.id===intent.beneficiaryId&&!candidate.dead);if(beneficiary)fireShotToPoint(enemy,beneficiary,'coin',285,0,{beneficiaryId:beneficiary.id});else fireShotToPoint(enemy,state.player,'coin',285);}else if(enemy.type==='skidder'){const dx=target.x-enemy.x;const dy=target.y-enemy.y;const magnitude=Math.hypot(dx,dy)||1;enemy.chargeDir={x:dx/magnitude,y:dy/magnitude};enemy.chargeTime=.46;enemy.state='charge';}else if(enemy.type==='mech'){if(intent.kind==='tree-shot')fireShotToPoint(enemy,target,'boss',300);else{for(const spread of[-.18,0,.18])fireShotToPoint(enemy,target,'boss',320,spread);}}enemy.intent=null;}function updateEnemies(dt){const p=state.player;if(!p)return;const globalPressure=pressure();for(const enemy of state.enemies){if(enemy.dead)continue;if(enemy.hitFlash>0)enemy.hitFlash-=dt;if(enemy.boosted>0)enemy.boosted-=dt;const spec=ENEMY_TYPES[enemy.type];const boost=enemy.boosted>0?1.45:1;if(enemy.type==='skidder'&&enemy.state==='charge'){enemy.x+=enemy.chargeDir.x*420*globalPressure*dt;enemy.y+=enemy.chargeDir.y*420*globalPressure*dt;enemy.chargeTime-=dt;if(dist(enemy,p)<enemy.r+p.r+4)damagePlayer(1,enemy);if(enemy.chargeTime<=0||enemy.x<35||enemy.x>W-35||enemy.y<85||enemy.y>H-30){enemy.state='recover';enemy.clock=.82;enemy.armor=0;}continue;}if(enemy.state==='recover'){enemy.clock-=dt;if(enemy.clock<=0){enemy.state='move';enemy.clock=.9+entityRand(enemy)*.5;enemy.armor=enemy.type==='skidder'?1:0;}continue;}enemy.clock-=dt;if(enemy.telegraph>0){enemy.telegraph-=dt;if(enemy.telegraph<=0){enemyAttack(enemy);if(enemy.state!=='charge')enemy.state='move';enemy.clock=(.95+entityRand(enemy)*.65)/globalPressure;}continue;}if(enemy.type==='drone'){enemy.angle+=dt*1.1*globalPressure;const tree=nearestLivingTree(enemy)||p;enemy.x+=Math.cos(enemy.angle)*34*dt;enemy.y+=Math.sin(enemy.angle)*34*dt;moveToward(enemy,tree,16*globalPressure,dt);}else if(['chair','lobbyist','foreman','broker'].includes(enemy.type)){const desired=230;const distanceToPlayer=dist(enemy,p);if(distanceToPlayer>desired+30)moveToward(enemy,p,spec.speed*boost*globalPressure,dt);else if(distanceToPlayer<desired-60){moveToward(enemy,{x:enemy.x+(enemy.x-p.x),y:enemy.y+(enemy.y-p.y)},spec.speed*.75*boost*globalPressure,dt);}}else{const tree=nearestLivingTree(enemy);const target=tree&&dist(enemy,tree)<dist(enemy,p)*.9?tree:p;moveToward(enemy,target,spec.speed*boost*globalPressure,dt);}enemy.x=clamp(enemy.x,34,W-34);enemy.y=clamp(enemy.y,88,H-35);if(enemy.clock<=0)beginTelegraph(enemy);}}function chooseBossIntent(boss){const p=state.player;if(boss.phase===3&&boss.attackCount%2===0){const tree=nearestLivingTree(boss);if(tree)return{kind:'tree',x:tree.x,y:tree.y,treeId:tree.id};}return{kind:'player',x:p.x,y:p.y};}function bossAttack(boss){const intent=boss.intent||chooseBossIntent(boss);const target={x:intent.x,y:intent.y};boss.attackCount+=1;if(boss.phase===1){for(const spread of[-.24,0,.24])fireShotToPoint(boss,target,'boss',330,spread);}else if(boss.phase===2){for(let i=0;i<8;i+=1){const angle=i*TAU/8;state.shots.push({x:boss.x,y:boss.y,vx:Math.cos(angle)*280,vy:Math.sin(angle)*280,r:7,life:3,kind:'paper',color:'#a9baff',friendly:false,owner:boss,damage:1,dead:false,spin:0,grazed:false,});}}else{if(intent.kind==='tree'){for(const spread of[-.10,.10])fireShotToPoint(boss,target,'boss',360,spread);}for(const spread of[-.30,-.10,.10,.30])fireShotToPoint(boss,state.player,'saw',338,spread);}boss.intent=null;boss.state='recover';boss.recover=boss.phase===1?.54:boss.phase===2?.46:.40;boss.clock=.9/pressure();}function updateBoss(dt){const boss=state.boss;const p=state.player;if(!boss||boss.dead||!p)return;if(boss.hitFlash>0)boss.hitFlash-=dt;boss.clock-=dt;boss.angle+=dt*(.55+boss.phase*.22);boss.x=720+Math.cos(boss.angle)*88;boss.y=H/2+Math.sin(boss.angle*.72)*145;if(boss.telegraph>0){boss.telegraph-=dt;if(boss.telegraph<=0)bossAttack(boss);return;}if(boss.state==='recover'){boss.recover-=dt;if(boss.recover<=0)boss.state='move';return;}if(boss.clock<=0){boss.intent=chooseBossIntent(boss);boss.telegraph=.66-boss.phase*.08;boss.state='telegraph';}}function updateShots(dt){const p=state.player;if(!p)return;for(const shot of state.shots){if(shot.dead)continue;shot.life-=dt;shot.spin=(shot.spin||0)+dt*8;if(shot.kind==='saw'&&!shot.friendly){const dx=p.x-shot.x;const dy=p.y-shot.y;const magnitude=Math.hypot(dx,dy)||1;shot.vx+=dx/magnitude*40*dt;shot.vy+=dy/magnitude*40*dt;}shot.x+=shot.vx*dt;shot.y+=shot.vy*dt;if(shot.life<=0||shot.x<-30||shot.x>W+30||shot.y<50||shot.y>H+30){shot.dead=true;continue;}if(shot.friendly){for(const enemy of state.enemies){if(enemy.dead||dist(shot,enemy)>=shot.r+enemy.r)continue;damageEnemy(enemy,shot.damage,{x:Math.sign(shot.vx),y:Math.sign(shot.vy)},{counter:true});shot.dead=true;break;}if(!shot.dead&&state.boss&&!state.boss.dead&&dist(shot,state.boss)<shot.r+state.boss.r){damageBoss(shot.damage,{x:Math.sign(shot.vx),y:Math.sign(shot.vy)},{counter:true});shot.dead=true;}continue;}if(shot.kind==='coin'&&shot.beneficiaryId){const beneficiary=state.enemies.find((enemy)=>enemy.id===shot.beneficiaryId&&!enemy.dead);if(beneficiary&&dist(shot,beneficiary)<shot.r+beneficiary.r){beneficiary.boosted=Math.max(beneficiary.boosted,2.2);shot.dead=true;for(let i=0;i<8;i+=1)spawnParticle(beneficiary.x,beneficiary.y,'#ffe56e',18+i*2);continue;}}for(const tree of state.trees){if(!tree.alive||!['mech','boss'].includes(shot.owner?.type))continue;if(dist(shot,tree)<shot.r+tree.r){damageTree(tree,1);shot.dead=true;break;}}if(shot.dead)continue;const playerDistance=dist(shot,p);const hitRadius=shot.r+p.r;if(playerDistance<hitRadius){if(shot.kind==='tape')state.slowTimer=2.2;damagePlayer(1,shot);shot.dead=true;continue;}if(p.dash&&!shot.grazed&&playerDistance<hitRadius+18){shot.grazed=true;state.stats.grazes+=1;p.flow=clamp(p.flow+6,0,100);state.score+=25;spawnParticle(p.x,p.y,'#b9f8ff',24);}}state.shots=state.shots.filter((shot)=>!shot.dead);}function updateSlashes(dt){for(const slash of state.slashes){slash.x=state.player.x;slash.y=state.player.y;slash.age+=dt;for(const shot of state.shots)counterShot(shot,slash);const d=DIRS[slash.direction];for(const enemy of state.enemies){if(enemy.dead||slash.hits.has(enemy.id)||!slashContains(slash,enemy))continue;slash.hits.add(enemy.id);damageEnemy(enemy,1.25,d,{counter:false});state.player.flow=clamp(state.player.flow+4,0,100);}if(state.boss&&!state.boss.dead&&!slash.hits.has(state.boss.id)&&slashContains(slash,state.boss)){slash.hits.add(state.boss.id);damageBoss(1.1,d,{counter:false});state.player.flow=clamp(state.player.flow+3,0,100);}for(const debris of state.debris){if(debris.dead||slash.hits.has(debris.id)||!slashContains(slash,debris))continue;slash.hits.add(debris.id);debris.hp-=1;if(debris.hp<=0){debris.dead=true;state.stats.deadwood+=1;state.score+=35;state.player.flow=clamp(state.player.flow+2,0,100);for(let i=0;i<8;i+=1)spawnParticle(debris.x,debris.y,'#c7955c',18+i*2);}}}state.slashes=state.slashes.filter((slash)=>slash.age<slash.life);}function spawnParticle(x,y,color,speed=30){const angle=Math.random()*TAU;const magnitude=speed*(.35+Math.random());state.particles.push({x,y,vx:Math.cos(angle)*magnitude,vy:Math.sin(angle)*magnitude,life:.25+Math.random()*.35,max:.6,color,r:1+Math.random()*2.4,});if(state.particles.length>260)state.particles.shift();}function updateParticles(dt){for(const particle of state.particles){particle.x+=particle.vx*dt;particle.y+=particle.vy*dt;particle.vx*=.94;particle.vy*=.94;particle.life-=dt;}state.particles=state.particles.filter((particle)=>particle.life>0);for(const trail of state.player?.trail||[])trail.life-=dt;if(state.player)state.player.trail=state.player.trail.filter((trail)=>trail.life>0);if(state.blockedHint){state.blockedHint.life-=dt;if(state.blockedHint.life<=0)state.blockedHint=null;}}function roomCleared(){return state.enemies.every((enemy)=>enemy.dead)&&(!state.boss||state.boss.dead);}function advanceRoom(){const alive=state.trees.filter((tree)=>tree.alive).length;const perfectGrove=alive===state.trees.length;state.stats.treesSaved+=alive;if(perfectGrove)state.stats.fullGroves+=1;state.score+=alive*120+Math.floor(state.player.flow)*4+(perfectGrove?250:0);state.worldsCleared+=1;const carry={hp:clamp(state.player.hp+(perfectGrove?1:0),1,state.player.maxHp),flow:state.player.flow*.35,};writeBest();setupRoom(state.worldDepth+1,carry);audio.room();toast(perfectGrove?`perfect grove · +1 heartwood · step ${Math.round(state.player.dashDistance)}px`:`room ${state.worldDepth} · step ${Math.round(state.player.dashDistance)}px`);}function endRun(reason){if(state.mode!=='playing')return;state.mode='gameover';writeBest();$('gameOverTitle').textContent=reason;$('gameOverSummary').textContent=`${state.worldsCleared} rooms · ${Math.floor(state.score)} score · ${state.stats.counters} counters · ${state.stats.grazes} grazes`;$('gameOver').classList.remove('hidden');}function update(dt){if(state.mode!=='playing')return;state.totalTime+=dt;state.roomTime+=dt;if(state.player.invuln>0)state.player.invuln-=dt;if(state.player.cutCooldown>0)state.player.cutCooldown-=dt;if(state.slowTimer>0)state.slowTimer-=dt;if(state.flash>0)state.flash-=dt;state.shake*=Math.pow(.02,dt);state.player.flow=Math.max(0,state.player.flow-dt*2.35);updateMovement(dt);updateSlashes(dt);updateEnemies(dt);updateBoss(dt);updateShots(dt);updateParticles(dt);if(roomCleared()){state.roomClearTimer+=dt;if(state.roomClearTimer>.92)advanceRoom();}else{state.roomClearTimer=0;}updateHud();}function drawBackground(){const palette=state.room?.palette||ROOM_BLUEPRINTS[0].palette;const gradient=ctx.createLinearGradient(0,0,W,H);gradient.addColorStop(0,palette[0]);gradient.addColorStop(.58,palette[1]);gradient.addColorStop(1,'#050a08');ctx.fillStyle=gradient;ctx.fillRect(0,0,W,H);ctx.strokeStyle=alpha(palette[2],.08);ctx.lineWidth=1;for(let x=0;x<W;x+=48){ctx.beginPath();ctx.moveTo(x,72);ctx.lineTo(x-80,H);ctx.stroke();}for(let y=98;y<H;y+=52){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y+32);ctx.stroke();}}function drawTree(tree){const palette=state.room.palette;ctx.save();ctx.translate(tree.x,tree.y);if(!tree.alive){ctx.fillStyle='#6e4b2f';ctx.fillRect(-7,-8,14,22);ctx.strokeStyle='#9d7047';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(-14,-8);ctx.lineTo(14,-8);ctx.stroke();ctx.restore();return;}const health=tree.hp/tree.maxHp;ctx.strokeStyle='#6f4f30';ctx.lineWidth=9;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(0,18);ctx.lineTo(0,-10);ctx.stroke();ctx.fillStyle=alpha(palette[2],.35+health*.45);for(let i=0;i<7;i+=1){const angle=i*TAU/7+tree.phase;ctx.beginPath();ctx.arc(Math.cos(angle)*15,-16+Math.sin(angle)*10,13,0,TAU);ctx.fill();}ctx.fillStyle='#dfffd8';ctx.fillRect(-18,28,36*health,3);ctx.restore();}function drawDebris(item){if(item.dead)return;ctx.save();ctx.translate(item.x,item.y);ctx.rotate(item.angle);ctx.strokeStyle='#8f6340';ctx.lineWidth=10;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(-item.r,0);ctx.lineTo(item.r,0);ctx.stroke();ctx.strokeStyle='#d09d67';ctx.lineWidth=2;for(let i=-1;i<=1;i+=1){ctx.beginPath();ctx.moveTo(i*7,-5);ctx.lineTo(i*7+3,5);ctx.stroke();}ctx.restore();}function drawSupportLinks(){ctx.save();ctx.setLineDash([4,7]);for(const chair of state.enemies.filter((enemy)=>!enemy.dead&&enemy.type==='chair')){for(const enemy of state.enemies){if(enemy.dead||enemy===chair||dist(chair,enemy)>165)continue;ctx.strokeStyle=alpha('#a9baff',.28);ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(chair.x,chair.y);ctx.lineTo(enemy.x,enemy.y);ctx.stroke();}}ctx.setLineDash([]);ctx.restore();}function drawIntentLine(entity,color){if(!entity.intent||entity.telegraph<=0)return;ctx.save();ctx.strokeStyle=alpha(color,.62);ctx.lineWidth=2.5;ctx.setLineDash([7,6]);ctx.beginPath();ctx.moveTo(entity.x,entity.y);ctx.lineTo(entity.intent.x,entity.intent.y);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=alpha(color,.18);ctx.beginPath();ctx.arc(entity.intent.x,entity.intent.y,17+Math.sin(performance.now()*.02)*3,0,TAU);ctx.fill();ctx.restore();}function drawEnemy(enemy){if(enemy.dead)return;const spec=ENEMY_TYPES[enemy.type];drawIntentLine(enemy,enemy.type==='broker'?'#ffe56e':enemy.type==='lobbyist'?'#d698ff':'#ff806f');ctx.save();ctx.translate(enemy.x,enemy.y);if(enemy.telegraph>0){ctx.strokeStyle=alpha('#ff7d67',.3+.45*Math.sin(performance.now()*.02));ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,enemy.r+11+enemy.telegraph*12,0,TAU);ctx.stroke();}if(enemy.state==='recover'){ctx.strokeStyle='#a8ffb5';ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,enemy.r+8,0,TAU);ctx.stroke();}if(shieldProvider(enemy)){ctx.strokeStyle=alpha('#a9baff',.65);ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,enemy.r+6,0,TAU);ctx.stroke();}ctx.fillStyle=enemy.hitFlash>0?'#ffffff':spec.color;ctx.beginPath();ctx.arc(0,0,enemy.r,0,TAU);ctx.fill();ctx.fillStyle='#101315';ctx.fillRect(-9,-5,5,4);ctx.fillRect(4,-5,5,4);ctx.strokeStyle='#101315';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(-7,7);ctx.lineTo(7,7);ctx.stroke();if(enemy.type==='feller'){ctx.strokeStyle='#d7e0e0';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(11,-14);ctx.lineTo(22,12);ctx.stroke();}else if(enemy.type==='foreman'){ctx.fillStyle='#222';ctx.fillRect(10,-3,16,7);}else if(enemy.type==='lobbyist'){ctx.fillStyle='#f6f0ff';ctx.fillRect(-13,15,26,8);}else if(enemy.type==='chair'){ctx.strokeStyle='#d5dcff';ctx.lineWidth=3;ctx.strokeRect(-15,-15,30,30);}else if(enemy.type==='broker'){ctx.fillStyle='#fff0a0';ctx.beginPath();ctx.arc(0,14,7,0,TAU);ctx.fill();}else if(enemy.type==='skidder'){ctx.strokeStyle=enemy.armor?'#f8dc83':'#704f20';ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,enemy.r+5,-1.1,1.1);ctx.stroke();}if(enemy.boosted>0){ctx.strokeStyle='#ffe56e';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,enemy.r+10+Math.sin(performance.now()*.02)*2,0,TAU);ctx.stroke();}ctx.fillStyle='#211515';ctx.fillRect(-enemy.r,-enemy.r-10,enemy.r*2,3);ctx.fillStyle='#a6ffad';ctx.fillRect(-enemy.r,-enemy.r-10,enemy.r*2*clamp(enemy.hp/enemy.maxHp,0,1),3);ctx.restore();}function drawBoss(){const boss=state.boss;if(!boss||boss.dead)return;drawIntentLine(boss,boss.phase===3?'#ff5f72':'#ff94b1');ctx.save();ctx.translate(boss.x,boss.y);if(boss.telegraph>0){ctx.strokeStyle=alpha('#ff668c',.65);ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,boss.r+18+boss.telegraph*20,0,TAU);ctx.stroke();}if(boss.state==='recover'){ctx.strokeStyle='#a8ffb5';ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,boss.r+12,0,TAU);ctx.stroke();}ctx.rotate(Math.sin(performance.now()*.002)*.08);ctx.fillStyle=boss.hitFlash>0?'#fff':'#ff668c';ctx.beginPath();ctx.arc(0,0,boss.r,0,TAU);ctx.fill();ctx.strokeStyle='#d9f6ff';ctx.lineWidth=7;for(let i=0;i<6;i+=1){const angle=i*TAU/6+performance.now()*.001;ctx.beginPath();ctx.moveTo(Math.cos(angle)*28,Math.sin(angle)*28);ctx.lineTo(Math.cos(angle)*57,Math.sin(angle)*57);ctx.stroke();}ctx.fillStyle='#10131a';ctx.fillRect(-17,-7,10,7);ctx.fillRect(7,-7,10,7);ctx.restore();}function drawShot(shot){ctx.save();ctx.translate(shot.x,shot.y);ctx.rotate(shot.spin||0);ctx.globalCompositeOperation='lighter';ctx.fillStyle=shot.color;ctx.shadowColor=shot.color;ctx.shadowBlur=10;if(shot.kind==='saw'){for(let i=0;i<8;i+=1){const angle=i*TAU/8;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(angle-.15)*13,Math.sin(angle-.15)*13);ctx.lineTo(Math.cos(angle+.15)*13,Math.sin(angle+.15)*13);ctx.closePath();ctx.fill();}}else if(shot.kind==='tape'){ctx.fillRect(-11,-4,22,8);}else if(shot.kind==='paper'){ctx.fillRect(-8,-6,16,12);}else if(shot.kind==='coin'){ctx.beginPath();ctx.arc(0,0,7,0,TAU);ctx.fill();ctx.fillStyle='#6c5712';ctx.fillRect(-1,-4,2,8);}else{ctx.beginPath();ctx.arc(0,0,shot.r,0,TAU);ctx.fill();}ctx.restore();if(!shot.friendly&&dist(shot,state.player)<185){const approach=shotApproachDirection(shot);ctx.save();ctx.fillStyle=alpha('#ffffff',.62);ctx.font='700 12px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(DIRS[approach].glyph,shot.x,shot.y-16);ctx.restore();}}function drawMachete(player){const activeSlash=state.slashes.at(-1);const direction=activeSlash?.direction||player.cutDirection||(player.facing==='left'?'left':'right');const d=DIRS[direction];ctx.save();ctx.rotate(Math.atan2(d.y,d.x));ctx.strokeStyle='#6e4e2c';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(8,4);ctx.lineTo(19,4);ctx.stroke();ctx.strokeStyle='#e8f3e9';ctx.lineWidth=5;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(18,4);ctx.lineTo(36,-1);ctx.stroke();ctx.lineCap='butt';ctx.restore();}function drawCounterCompass(player){let nearest=null;let nearestDistance=Infinity;for(const shot of state.shots){if(shot.friendly)continue;const distance=dist(shot,player);if(distance<nearestDistance&&distance<145){nearest=shot;nearestDistance=distance;}}if(!nearest)return;const approach=shotApproachDirection(nearest);const d=DIRS[approach];ctx.save();ctx.strokeStyle=alpha('#fffce0',clamp(1-nearestDistance/155,.18,.8));ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(d.x*24-d.y*6,d.y*24+d.x*6);ctx.lineTo(d.x*31,d.y*31);ctx.lineTo(d.x*24+d.y*6,d.y*24-d.x*6);ctx.stroke();ctx.restore();}function drawPlayer(){const p=state.player;if(!p)return;for(const trail of p.trail){ctx.fillStyle=alpha('#8dff9b',clamp(trail.life/.18,0,1)*.20);ctx.beginPath();ctx.arc(trail.x,trail.y,p.r*1.15,0,TAU);ctx.fill();}ctx.save();ctx.translate(p.x,p.y);if(p.dash){ctx.rotate(Math.atan2(p.dash.dir.y,p.dash.dir.x));ctx.scale(1.34,.79);}if(p.invuln>0&&Math.floor(p.invuln*18)%2===0)ctx.globalAlpha=.35;ctx.fillStyle='#8dff9b';ctx.shadowColor='#8dff9b';ctx.shadowBlur=14;ctx.beginPath();ctx.arc(0,0,p.r,0,TAU);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#102014';ctx.fillRect(-7,-5,4,4);ctx.fillRect(3,-5,4,4);ctx.fillStyle='#d6f7cb';ctx.beginPath();ctx.moveTo(-6,-13);ctx.lineTo(-1,-23);ctx.lineTo(2,-12);ctx.fill();ctx.beginPath();ctx.moveTo(2,-13);ctx.lineTo(8,-21);ctx.lineTo(8,-10);ctx.fill();ctx.restore();ctx.save();ctx.translate(p.x,p.y);drawMachete(p);drawCounterCompass(p);ctx.restore();}function drawSlashes(){for(const slash of state.slashes){const d=DIRS[slash.direction];const t=slash.age/slash.life;ctx.save();ctx.translate(slash.x,slash.y);ctx.rotate(Math.atan2(d.y,d.x));ctx.strokeStyle=alpha('#ecffe9',1-t);ctx.lineWidth=8*(1-t)+2;ctx.lineCap='round';ctx.beginPath();ctx.arc(6,0,54,-.62,.62);ctx.stroke();ctx.strokeStyle=alpha('#85ffab',.7*(1-t));ctx.lineWidth=2;ctx.beginPath();ctx.arc(6,0,66,-.55,.55);ctx.stroke();ctx.restore();}}function drawParticles(){ctx.save();ctx.globalCompositeOperation='lighter';for(const particle of state.particles){ctx.fillStyle=alpha(particle.color,clamp(particle.life/particle.max,0,1));ctx.beginPath();ctx.arc(particle.x,particle.y,particle.r,0,TAU);ctx.fill();}ctx.restore();}function drawBlockedHint(){if(!state.blockedHint)return;ctx.save();ctx.strokeStyle=alpha('#ffc86e',clamp(state.blockedHint.life/.28,0,1));ctx.lineWidth=4;ctx.beginPath();ctx.arc(state.blockedHint.x,state.blockedHint.y,24,0,TAU);ctx.stroke();ctx.restore();}function render(){const shakeX=state.shake?(Math.random()-.5)*state.shake:0;const shakeY=state.shake?(Math.random()-.5)*state.shake:0;const scale=window.SylvariaDisplayScale?.scale||1;ctx.save();ctx.setTransform(scale,0,0,scale,0,0);ctx.translate(shakeX,shakeY);drawBackground();for(const tree of state.trees)drawTree(tree);for(const item of state.debris)drawDebris(item);drawSupportLinks();for(const enemy of state.enemies)drawEnemy(enemy);drawBoss();for(const shot of state.shots)drawShot(shot);drawPlayer();drawSlashes();drawParticles();drawBlockedHint();if(state.roomClearTimer>0){ctx.fillStyle=alpha('#8dff9b',clamp(state.roomClearTimer,0,.7)*.25);ctx.fillRect(0,0,W,H);ctx.fillStyle='#efffed';ctx.font='700 28px system-ui';ctx.textAlign='center';ctx.fillText('GROVE SECURED',W/2,H/2);}if(state.flash>0){ctx.fillStyle=`rgba(255,255,255,${state.flash})`;ctx.fillRect(0,0,W,H);}ctx.restore();}function updateHud(force=false){if(!state.player||!state.room)return;if(force){$('roomTitle').textContent=state.room.title;$('roomTask').textContent=state.room.hint;$('roomKicker').textContent=`room ${String(state.worldDepth).padStart(3, '0')} · countercut expedition`;$('biomeBadge').textContent=state.room.subtitle;}$('score').textContent=Math.floor(state.score);$('time').textContent=formatTime(state.totalTime);$('integrity').textContent='◆'.repeat(Math.max(0,state.player.hp))+'◇'.repeat(Math.max(0,state.player.maxHp-state.player.hp));$('flowState').textContent=`${Math.round(state.player.flow)}%`;$('dashState').textContent=state.slowTimer>0?'RED TAPE':state.player.dash?'STEP':`${Math.round(state.player.dashDistance)}px`;$('treeState').textContent=`${state.trees.filter((tree)=>tree.alive).length}/${state.trees.length}`;$('counterState').textContent=String(state.stats.counters);const graze=$('grazeState');if(graze)graze.textContent=String(state.stats.grazes);$('bossWrap').hidden=!state.boss;if(state.boss){$('bossState').textContent=state.boss.dead?'DOWN':`P${state.boss.phase} · ${state.boss.state === 'recover' ? 'OPEN · ' : ''}${Math.ceil(state.boss.hp)}/${state.boss.maxHp}`;}}function formatTime(seconds){const minutes=Math.floor(seconds/60);const remaining=seconds-minutes*60;return `${minutes}:${remaining.toFixed(1).padStart(4, '0')}`;}function toast(text){const element=$('toast');if(!element)return;element.textContent=text;element.classList.add('show');clearTimeout(toast._timer);toast._timer=setTimeout(()=>element.classList.remove('show'),1800);}function startRun(mode='run'){state.mode='playing';state.runMode=mode;state.score=0;state.totalTime=0;state.worldsCleared=0;state.stats=freshStats();document.querySelectorAll('.screen').forEach((element)=>element.classList.add('hidden'));$('hud').classList.remove('hidden');setupRoom(1);canvas.focus();audio.ensure();toast('WASD step-dashes · arrows cut · M mutes');}function snapshot(){return{title:'Sylvaria',version:VERSION,mode:state.mode,sectorIndex:state.roomIndex,worldDepth:state.worldDepth,worldsCleared:state.worldsCleared,score:state.score,fps:state.fps,room:state.room?{title:state.room.title,subtitle:state.room.subtitle,seed:state.room.seed}:null,player:state.player?{x:state.player.x,y:state.player.y,hp:state.player.hp,maxHp:state.player.maxHp,flow:state.player.flow,dashDistance:state.player.dashDistance,dashing:Boolean(state.player.dash),bufferedMove:state.moveBuffer?.key||null,}:null,enemies:state.enemies.filter((enemy)=>!enemy.dead).map((enemy)=>({id:enemy.id,type:enemy.type,x:enemy.x,y:enemy.y,r:enemy.r,hp:enemy.hp,maxHp:enemy.maxHp,pattern:enemy.state,boosted:enemy.boosted>0,shielded:Boolean(shieldProvider(enemy)),intent:enemy.intent?{...enemy.intent}:null,})),boss:state.boss?{x:state.boss.x,y:state.boss.y,r:state.boss.r,hp:state.boss.hp,maxHp:state.boss.maxHp,phase:state.boss.phase,dead:state.boss.dead,state:state.boss.state,vulnerable:state.boss.state==='recover',intent:state.boss.intent?{...state.boss.intent}:null,}:null,trees:state.trees.map((tree)=>({id:tree.id,x:tree.x,y:tree.y,hp:tree.hp,alive:tree.alive})),debris:state.debris.map((item)=>({id:item.id,x:item.x,y:item.y,hp:item.hp,dead:item.dead})),shots:state.shots.map((shot)=>({x:shot.x,y:shot.y,kind:shot.kind,friendly:shot.friendly,beneficiaryId:shot.beneficiaryId||null,counterQuality:shot.counterQuality||null,})),stats:{...state.stats,casts:state.stats.cuts},dashDistance:state.player?.dashDistance||0,roomCleared:roomCleared(),slowTimer:state.slowTimer,muted:state.muted,portalOpen:false,portalReady:false,portalPhase:'retired',targets:[],stones:0,stoneQuota:0,};}function placeDeadwoodAhead(direction='right',distance=34){const d=DIRS[direction]||DIRS.right;const p=state.player;const item={id:`playtest-deadwood-${performance.now()}`,x:clamp(p.x+d.x*distance,45,W-45),y:clamp(p.y+d.y*distance,95,H-45),r:18,hp:2,dead:false,angle:0,};state.debris.push(item);return snapshot();}function spawnCounterShot(direction='right',distance=92){const d=DIRS[direction]||DIRS.right;const p=state.player;const sx=p.x+d.x*distance;const sy=p.y+d.y*distance;state.shots.push({x:sx,y:sy,vx:-d.x*245,vy:-d.y*245,r:6,life:2,kind:'nail',color:'#ffad79',friendly:false,owner:null,damage:1,dead:false,spin:0,grazed:false,});return snapshot();}window.__MOSSLIGHT_PLAYTEST__={title:'Sylvaria',version:VERSION,roomCount:10,roomTitles:ROOM_BLUEPRINTS.map((room)=>room.title),snapshot,setRoom(index,depth=index+1){setupRoom(depth||index+1);state.mode='playing';return snapshot();},completeRoom(){state.enemies.forEach((enemy)=>{enemy.dead=true;});if(state.boss)state.boss.dead=true;return snapshot();},defeatBoss(){if(state.boss)state.boss.dead=true;return snapshot();},advance(){advanceRoom();return snapshot();},cut(direction){cut(direction);return snapshot();},dash(direction){dashStep(DIRS[direction]||DIRS.right);return snapshot();},requestDash(key){requestDash(key,true);return snapshot();},spawnCounterShot,placeDeadwoodAhead,damagePlayer(amount=1){damagePlayer(amount);return snapshot();},setFlow(value){state.player.flow=clamp(value,0,100);return snapshot();},roomBlueprint(depth){return roomBlueprint(depth);},forceBossPhase(phase){if(!state.boss)return snapshot();state.boss.phase=clamp(phase,1,3);state.boss.hp=state.boss.maxHp*(phase===1?.9:phase===2?.55:.25);state.boss.state='move';state.boss.clock=.01;return snapshot();},};window.MosslightExpedition={atlasCount:1000,summary:()=>({atlasCount:1000,worlds:Array.from({length:10},(_,index)=>({index:index+1})),deck:{cursor:10}}),newRun:()=>({}),};window.MosslightDirector={movementPatterns:['telegraph-lock','cardinal-counter','orbit','charge-recover','shield-support','subsidy-intercept','tree-pressure'],powerups:[],summary:()=>ROOM_BLUEPRINTS.map((room,index)=>({room:index+1,situation:room.subtitle})),};document.addEventListener('keydown',(event)=>{const key=normKey(event.key);if(['w','a','s','d','arrowup','arrowdown','arrowleft','arrowright',' ','shift','enter'].includes(key))event.preventDefault();if(key==='m'){state.muted=!state.muted;toast(state.muted?'sound muted':'sound on');if(!state.muted)audio.ensure();return;}if(state.mode==='menu'&&key==='enter'){startRun('run');return;}if(key==='p'&&state.mode==='paused'){state.mode='playing';$('pauseScreen').classList.add('hidden');canvas.focus();return;}if(state.mode!=='playing')return;if(event.repeat)return;if(MOVE_DIRS[key]){state.heldMoves.add(key);state.heldOrder=state.heldOrder.filter((entry)=>entry!==key);state.heldOrder.push(key);requestDash(key,true);return;}if(CUT_KEYS[key]){cut(CUT_KEYS[key]);return;}if(key==='p'){state.mode='paused';$('pauseScreen').classList.remove('hidden');}});document.addEventListener('keyup',(event)=>{const key=normKey(event.key);if(MOVE_DIRS[key]){state.heldMoves.delete(key);state.heldOrder=state.heldOrder.filter((entry)=>entry!==key);}});window.addEventListener('blur',()=>{state.heldMoves.clear();state.heldOrder=[];});$('start')?.addEventListener('click',()=>startRun('run'));$('explore')?.addEventListener('click',()=>startRun('explore'));$('restartRun')?.addEventListener('click',()=>{$('gameOver').classList.add('hidden');startRun('run');});$('menuFromGameOver')?.addEventListener('click',()=>{state.mode='menu';$('gameOver').classList.add('hidden');$('title').classList.remove('hidden');$('hud').classList.add('hidden');});$('resume')?.addEventListener('click',()=>{state.mode='playing';$('pauseScreen').classList.add('hidden');canvas.focus();});$('howBtn')?.addEventListener('click',()=>{$('title').classList.add('hidden');$('howScreen').classList.remove('hidden');});$('controlsBtn')?.addEventListener('click',()=>{$('title').classList.add('hidden');$('controlsScreen').classList.remove('hidden');});document.querySelectorAll('[data-back]').forEach((button)=>button.addEventListener('click',()=>{document.querySelectorAll('.screen').forEach((element)=>element.classList.add('hidden'));$('title').classList.remove('hidden');}));$('bestRun').textContent=`best · room ${state.best.room || 0} · ${state.best.score || 0} score`;let accumulator=0;let last=performance.now();let fpsClock=performance.now();let frames=0;function frame(now){const elapsed=Math.min(.08,(now-last)/1000);last=now;accumulator+=elapsed;while(accumulator>=FIXED_DT){update(FIXED_DT);accumulator-=FIXED_DT;}render();frames+=1;if(now-fpsClock>=500){state.fps=frames*1000/(now-fpsClock);frames=0;fpsClock=now;}requestAnimationFrame(frame);}setupRoom(1);state.mode='menu';drawBackground();requestAnimationFrame(frame);})();